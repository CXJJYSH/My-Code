16n.(Optional)

Generally, we create tests for each file of code.

Examples: money.js => moneyTest.js and cart.js => cartTest.js

If you want more practice, choose some other files like **products.js** or **paymentSummary.js** and create tests for these.

(

Note:
Creating tests takes time so this exercise is optional.
There's no solution code for this exercise, but you can check your code by re-running the tests.

)

**product.js**应该mock一下getItem的products，然后调用getProduct(productId)函数，然后看返回的对象的属性对不对得上。

**paymentSummary.js**看一眼看不出来具体要测试哪些地方。

初步可以检查简单的几个文本是否呈现对了，如下：

<!--
import { renderPaymentSummary } from "../../scripts/checkout/paymentSummary.js";
import { cart } from "../../data/cart.js";

describe("renderPaymentSummary", () => {
  it("displays the correct payment summary", () => {
    document.querySelector(".js-test-container").innerHTML = `
      <div class="js-payment-summary"></div>
    `;

    renderPaymentSummary();

    expect(
      document.querySelector(".js-payment-summary").innerText
    ).toContain("Items (2):");

    expect(
      document.querySelector(".js-payment-summary").innerText
    ).toContain("Shipping & handling:");

    expect(
      document.querySelector(".js-payment-summary").innerText
    ).toContain("Total before tax:");

    expect(
      document.querySelector(".js-payment-summary").innerText
    ).toContain("Estimated tax (10%):");

    expect(
      document.querySelector(".js-payment-summary").innerText
    ).toContain("Order total:");
  });
});
-->

然后又要看一些其它地方，所以还可以细化，现在还不明白到底还有哪些地方。
https://chatgpt.com/c/6aa007a6-35c4-83ec-a6c8-d5401430f95a

**Arrange => Act => Assert**

Arrange
准备 cart、DOM

↓

Act
调用 renderPaymentSummary()

↓

Assert
检查页面显示的金额是否正确

<!-- 2026.09.08 20:52 -->

<!-- 2026.09.08 21:13 -->
