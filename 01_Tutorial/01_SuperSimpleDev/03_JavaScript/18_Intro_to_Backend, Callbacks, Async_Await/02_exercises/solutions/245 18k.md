If you have failing tests, you probably just need to add:

```javascript
beforeAll(async () => {
  await loadProductsFetch();
});
```

at the start of your tests.

好难绷。

17j的测试有好多没通过，当时直接懒得管了，直接用17i的代码了。

<!-- 2026.09.16 15:16 -->
