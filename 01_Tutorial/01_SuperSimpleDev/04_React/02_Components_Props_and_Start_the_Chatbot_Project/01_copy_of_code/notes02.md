**The simple chatbot**<br>

1. Get today's date
2. Flip a coin
3. Roll a dice

In the future, we can switch this to a more powerful AI (Artificial Intelligence).

**Components**<br>

Component = a piece of the website

When building websites:<br>

- It's better to split up the website into pieces.
- So we can work on a small piece of the website at a time.

**Create First Component**

<u>Main Idea of React</u><br>
We can create our own HTML elements.

**Fragment**<br>
`<>``</>`

In React, we still use `<div>` for layout purposes.

Components are designed to be reusable.

**Shortcuts in React**<br>

1. Destructuring
2. Use if-statements directly inside JSX
   Guard Operator(&&)

   ```js
   const result = value1 && value2;
   ```

   - If value1 is true, the result will be value2.
   - This works just like an if-statement.

<u>Best Practice</u><br>
Use a component (App) to create the website.

**Next Lesson: State**<br>
Make our website interactive.

**In this lesson:**<br>

1. Component = a piece of the website
2. Component - create our own HTML elements
3. Started the Chatbot Project
4. Split the Chatbot into `<ChatInput> <ChatMessage>`
5. props = make components reusable
6. Destructuring, Guard Operator(`&&`)
7. Code cleanup
8. Created `<App>` component

**Premium Course**<br>
<u>Disclaimer:</u><br>
Currently, 11 lessons.
Simon will add more lessons in the future.

<!-- 2026.09.21 21:26 -->
